{
  "name": "who-am-i",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "test": "node --test",
    "seed:decks": "node server/scripts/seed-system-decks.js",
    "build": "npm --prefix frontend install && npm --prefix frontend run build",
    "postinstall": "npm --prefix frontend install && npm --prefix frontend run build",
    "pages:build": "node scripts/build-pages-site.mjs"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "express": "^4.21.2"
  }
}
